package validator

import "testing"

//tests to do
//validator creation
//validator correct hashrate
//validator incorrect hashrate
//validator blockheader update
//validator blockheader update and outdated hash submission

//create a new validator and check to see if a message can be passed to it
func my_test(t *testing.T) {
}


//create a new validator and then check to see if the block header can be updated
func my_test(t *testing.T) {
}


//create a new validator and check to if a hash can be validated
func my_test(t *testing.T) {
}
